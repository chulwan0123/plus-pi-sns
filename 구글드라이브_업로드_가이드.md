# ☁️ 구글드라이브 업로드 가이드

산출물(`exports/` = `구글드라이브_최종본/` 미러)을 **구글드라이브의 가이드별 폴더**에 올리는 표준 절차.
업로드는 **rclone**으로 한다. (클라우드 커넥터 API는 대용량 업로드/이름변경/삭제를 지원하지 않아 rclone을 쓴다.)

---

## 0. 어디에 올리는가 (목적지)

- **상위(부모) 폴더** — "PLUS 콘텐츠" 루트. 이 안에 가이드 폴더들이 있다.
  - ID: `1-Qlr0NjrSOH1Isp81-HbiY0SVbo_Trae`
- **가이드별 폴더**(이름 = 탭 라벨과 동일):

| 가이드 | 드라이브 폴더명 | 폴더 ID |
|---|---|---|
| 01 | 파이 이용가이드 01_시작하기 | `16VfvLbW4h_KDBCWF5RtGeXmxMbR0CNpo` |
| 02 | 파이 이용가이드 02_파이 간편신고 알아보기 | `194QIcpprp0QcAyh9-dTxxHRMHiJ5mcY4` |
| 03 | 파이 이용가이드 03_증여세 신고하기 | `1X8hWc_vxw0j3ju5pGITHmdDkb92XkRiw` |
| 04 | 파이 이용가이드 04_우리아이 첫 투자 미국 ETF 입문 가이드 | `1IbvQOsflsXHMeD4_Q5JJ3K4eUfu5-ALn` |
| (에셋) | 무료 배경음악 에셋 | `1WVQQTPd17gpKZpZ_rV2yX-bxvAozjoqW` |
| (에셋) | 동영상 에셋 | `1mygkMwzAQlJUbOlLNYjbxHhhUxcaoTjp` |

> 새 가이드(예: 05)는 이 상위 폴더 아래에 같은 규칙명으로 **새 폴더**를 만들어 올린다.
> **각 가이드 폴더 안에 넣는 것**: mp4들(클립·합본·9:16·BGM) + 대지별 이미지(`_0N.png`) + `_표지.png` + `인스타그램_캡션.md`.

---

## 1. rclone 준비 (최초 1회)

```bash
brew install rclone

# 구글드라이브 인증(브라우저 1회 승인) → remote 이름은 gdrive 로 통일
rclone authorize "drive"      # 브라우저가 열리면 로그인/허용 → 토큰 출력
# 위 토큰(JSON 한 줄)으로 remote 생성 (토큰 값은 노출 주의):
#   rclone config create gdrive drive scope drive token '<붙여넣은 JSON>'
```

- remote 이름: **`gdrive`**, scope: **`drive`**(전체).
- 확인: `rclone listremotes` → `gdrive:` 가 보이면 OK.
- 접근 테스트: `rclone lsd "gdrive:" --drive-root-folder-id 1-Qlr0NjrSOH1Isp81-HbiY0SVbo_Trae`

> 인증이 만료되면: `rclone config reconnect gdrive:` (다시 브라우저 승인).

---

## 2. 올리기 / 교체하기

가이드 폴더는 이미 있으므로(위 표), **폴더 ID를 직접 지정**해 올리는 게 가장 안전하다.
파일명이 드라이브의 것과 1:1 같으면 `copy --checksum` 이 **바뀐 파일만 덮어쓴다**(캡션 등 동일 파일은 스킵).

```bash
PARENT=1-Qlr0NjrSOH1Isp81-HbiY0SVbo_Trae
T="파이 이용가이드 04_우리아이 첫 투자 미국 ETF 입문 가이드"

# (a) 미러 → 드라이브 (변경분만 업로드)
rclone copy "구글드라이브_최종본/$T" "gdrive:$T" \
  --drive-root-folder-id "$PARENT" --checksum --transfers 4 --exclude "**/.DS_Store" -P

# (b) 무결성 검증 (0 differences 여야 함)
rclone check "구글드라이브_최종본/$T" "gdrive:$T" \
  --drive-root-folder-id "$PARENT" --checksum
```

### 새 폴더부터 만들 때 (예: 신규 가이드)
```bash
NEW="파이 이용가이드 05_세금 없이 물려주기"
rclone mkdir "gdrive:$NEW" --drive-root-folder-id "$PARENT"
rclone copy "구글드라이브_최종본/$NEW" "gdrive:$NEW" --drive-root-folder-id "$PARENT" --checksum -P
# 새 폴더 ID/URL 확인
rclone lsf "gdrive:" --drive-root-folder-id "$PARENT" --dirs-only --format "ip" | grep -F "$NEW"
```

### 특정 파일 삭제 (예: 대지별 png 제거, 표지만 남기기)
```bash
# 기본 동작은 "휴지통 이동"(복구 가능). 영구삭제 아님.
rclone delete "gdrive:$T" --drive-root-folder-id "$PARENT" --include "*_0[0-9].png"
```

### 폴더 통째 정리(구버전 폴더 제거)
```bash
rclone purge "gdrive:<구폴더명>" --drive-root-folder-id "$PARENT"   # 휴지통 이동
```

---

## 3. 규칙 · 주의사항

1. **폴더/파일명은 드라이브 폴더명 기준으로 통일**(탭 라벨과 동일). → [README](README.md) 2절 표.
2. **교체 = 파일명 1:1 매칭 + `--checksum`**. 내용이 바뀌면 크기·해시가 달라 자동 재업로드된다.
3. **삭제는 기본 휴지통**(`--drive-use-trash` 기본 true). `--drive-use-trash=false`(영구삭제)는 쓰지 않는다.
4. **업로드 후 반드시 `rclone check … --checksum` 으로 `0 differences` 확인.**
5. **macOS 파일명 정규화(NFD/NFC)** 주의: `--include "패턴"` 이나 파이썬 문자열 비교가 한글에서 오탐날 수 있다. 가장 신뢰할 검증은 **폴더 대 폴더 `rclone check`**(위 (b)).
   - ⚠️ **미러 갱신은 `cp` 단독 금지**: 미러(`구글드라이브_최종본/…`)에 이미 있는 한글 파일을 `cp -f`로 덮어쓸 때, 기존 파일명이 다른 정규화(NFD/NFC)면 **덮어쓰기가 조용히 실패**해 옛 파일이 남는다. 그러면 미러가 stale → 그걸 업로드하고, `rclone check`는 stale(미러) vs stale(Drive)라 **0 differences로 오탐**한다(과거 무음 9:16이 표지 없는 옛 버전으로 남았던 원인). **반드시 지워고 새로 복사**:
     ```bash
     rm -f "구글드라이브_최종본/$T/"*.mp4 "구글드라이브_최종본/$T/"*.png   # 캡션 md는 유지
     cp exports/"$T"*.mp4 exports/"$T"*.png "구글드라이브_최종본/$T/"
     # 검증: 미러 각 mp4 길이가 exports와 같은지 ffprobe로 확인
     ```
   - ⚠️ **Drive 미리보기 캐시**: `rclone copy`로 내용만 덮으면 파일 ID가 유지돼 Drive 웹 미리보기·썸네일이 **옛 영상**을 계속 보여줄 수 있다. 확실히 갱신하려면 **폴더 내 파일 삭제 후 새 업로드**(새 파일 ID): `rclone delete "gdrive:$T" --drive-root-folder-id "$PARENT"` → `rclone copy …`.
6. **토큰/자격증명은 출력·커밋 금지.** 임시 토큰 파일은 사용 후 삭제.
7. **`.DS_Store` 등 OS 메타파일 업로드 금지** — `rclone copy … --exclude "**/.DS_Store"`. 실수로 올라갔으면 `rclone deletefile "gdrive:<폴더>/.DS_Store" --drive-root-folder-id "$PARENT"`.
8. 커넥터(claude.ai Google Drive)로도 **읽기/폴더목록**은 가능하나, 대용량 업로드·이름변경·삭제는 불가 → 업로드는 rclone.

---

## 4. 현재 업로드 상태(기준 스냅샷)

| 폴더 | 파일 수 | 구성 |
|---|---|---|
| 02 | 17 | 클립6 + 이미지6 + 합본 + 9:16 + BGM + 표지 + 캡션 |
| 03 | 21 | 클립8 + 이미지8 + 합본 + 9:16 + BGM + 표지 + 캡션 |
| 04 | 23 | 클립9 + 이미지9 + 합본 + 9:16 + BGM + 표지 + 캡션 |
| 05 | 19 | 클립7 + 이미지7 + 합본 + 9:16 + BGM(hopeful_open_road) + 표지 + 캡션 |
