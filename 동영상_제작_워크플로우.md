# 동영상·이미지 제작 워크플로우 (고정 규격)

파이 이용가이드(탭1·2·3·4)의 대지(1080×1350 캔버스)를 **개별 클립 / 합본 / 9:16 유튜브 / BGM / 이미지**로 내보내는 표준 절차. **앞으로 모든 가이드 영상은 이 방식으로 만든다.**

---

## 0. 공통 규격
- **캔버스**: 1080 × 1350 (4:5, 인스타 세로). 각 `.tab-panel > .canvas` 1장 = 1 대지.
- **영상**: H.264(libx264), `crf 20`, `preset veryfast`, `yuv420p`, **30fps**, `-movflags +faststart`.
- **캡처 배율**: Playwright `deviceScaleFactor: 2`(선명), 녹화 사이즈는 1080×1350.
- **도구**: Playwright(Chromium) + `ffmpeg-static`. 스크립트는 `tools/`.
- **BGM 원본**: `~/Downloads/음원/`. **가이드별 고정 배분**: 02 = `warm_fintech…`, 03 = `youthful_fintech_nudisco…`, 04 = `digital_hopeful_corporate…`.
- **가이드↔탭↔해시 매핑 / 폴더·파일 이름 규칙**은 [README](README.md) 2절 표 참조(예: 02→panel `tab2`/`#guide`).

### 두 가지 핵심 원칙
1. **잘리지 않음(no cut)** — 진입 애니메이션은 반드시 **0프레임부터** 시작. 배속(빨리감기) 금지. 애니메이션이 끝난 뒤 컷.
2. **최소 초 보장(min duration)** — 짧은 클립은 **최종 프레임을 홀드**해 최소 길이를 채움. (내용 페이지 3.5초, 표지 2초, 마퀴/영상은 자연 길이)

---

## 1. 준비
```bash
# 로컬 서버 (프로젝트 루트)
python3 -m http.server 8778

# 도구 설치 (tools/ 안에서 1회)
cd tools
npm i playwright ffmpeg-static
npx playwright install chromium
```

---

## 2. 개별 클립 내보내기 — `tools/export-clips.js`
각 대지를 격리(탭바 숨김·해당 캔버스만 표시)해 녹화. **hold/release** 기법으로 애니메이션을 0프레임부터 캡처한다.

- **HOLD**: 로드 중 `.is-playing` 클래스를 계속 제거하고 `<video>`를 0으로 되감아 정지 상태 유지.
- **RELEASE**: 안정화(≈450ms) 후 `.has-motion/.result-motion`에 `.is-playing` 부여 + 영상 `play()` → 이때부터 애니메이션 실제 시작.
- **완료 감지**: 릴리즈 이후 유한 애니메이션·영상이 멈추는 시점을 디바운스로 감지(무한 마퀴 애니메이션은 제외).
- **minMs**: 완료가 minMs보다 이르면 최종 프레임을 minMs까지 홀드.
- webm 녹화 → ffmpeg로 릴리즈 지점부터 트림해 mp4.

```bash
GUIDE=04 node export-clips.js      # 가이드 04 전체 (GUIDE=02|03|04)
GUIDE=04 node export-clips.js 7    # 04의 idx=7 대지 하나만 재생성
```
가이드 선택은 **환경변수 `GUIDE=02|03|04`**. 스크립트 상단 `GUIDES` 객체에 가이드별 `panel`·`hash`·`mins`(각 대지 minMs, 마지막 0=마무리 영상 자연길이)가 정의돼 있어, 새 가이드는 여기 한 줄만 추가한다.
출력: `exports/파이 이용가이드 0N_<제목>_0M.mp4`

---

## 3. 합본 + 9:16 + BGM — `tools/build-combine.sh` (3·4·5 자동화)
**표지 2초** + 내용 클립들(각자 자연/최소 길이, 컷·배속 없음)을 이어붙인 뒤, 9:16 패딩과 BGM까지 한 번에 만든다.

```bash
# 사용법: build-combine.sh "<제목(확장자 제외)>" <표지초> <내용 마지막 대지번호> <BGM경로>
bash tools/build-combine.sh "파이 이용가이드 04_우리아이 첫 투자 미국 ETF 입문 가이드" \
  2.0 09 ~/Downloads/음원/digital_hopeful_corporate_instrumental_free.mp3
# → 합본 .mp4 + _9x16(youtube).mp4 + _9x16(youtube)_bgm.mp4 생성
```
> ⚠️ 스크립트 주의점(반영됨): macOS `seq -w 2 9`는 제로패딩을 안 하므로 `printf "%02d"`로 `_02.._0N` 생성. 길이 측정은 `ffprobe`로(`ffmpeg -i`는 출력 없이 exit 1이라 `set -e`에서 죽음).

아래 4·5절은 build-combine.sh가 내부적으로 하는 일(수동 참고용)이다.

### 3-1. 합본만 수동으로 (내부 동작)
```bash
FF=ffmpeg                                         # 설치된 ffmpeg(권장) 또는 ffmpeg-static
T="파이 이용가이드 04_우리아이 첫 투자 미국 ETF 입문 가이드"
cd exports

# (a) 표지 2초 세그먼트: 표지 클립 앞 2초, 또는 표지 이미지를 2초로
"$FF" -y -t 2.0 -i "${T}_01.mp4" \
  -c:v libx264 -crf 20 -preset veryfast -pix_fmt yuv420p -vf fps=30 -movflags +faststart cover2s.mp4
#   (표지를 정지 이미지로 쓸 때)
# "$FF" -y -loop 1 -t 2.0 -i cover.png -vf "scale=1080:1350,fps=30,format=yuv420p" \
#   -c:v libx264 -crf 20 -preset veryfast -movflags +faststart cover2s.mp4

# (b) concat 리스트: 표지2초 + _02.._0N
printf "file '%s'\n" "$PWD/cover2s.mp4" > list.txt
for n in 02 03 04 05 06 07 08 09; do
  [ -f "${T}_${n}.mp4" ] && printf "file '%s'\n" "$PWD/${T}_${n}.mp4" >> list.txt
done

# (c) 이어붙이기(재인코딩)
"$FF" -y -f concat -safe 0 -i list.txt \
  -c:v libx264 -crf 20 -preset veryfast -pix_fmt yuv420p -vf fps=30 -movflags +faststart "$T.mp4"
```
출력: `exports/파이 이용가이드 0N_<제목>.mp4`

---

## 4. 9:16 유튜브(쇼츠) 버전
합본(1080×1350)을 **1080×1920**으로 흰 여백 패딩(상·하 285px).
```bash
"$FF" -y -i "$T.mp4" \
  -vf "pad=1080:1920:0:285:color=white,format=yuv420p" \
  -c:v libx264 -crf 20 -preset veryfast -movflags +faststart -an "${T}_9x16(youtube).mp4"
```
출력: `..._9x16(youtube).mp4`

---

## 5. 배경음악(BGM) 버전
9:16 영상에 BGM을 얹고 **끝 2.5초 페이드아웃**. BGM이 짧으면 루프.
```bash
BGM=~/Downloads/음원/digital_hopeful_corporate_instrumental_free.mp3   # 가이드별: 02 warm_fintech / 03 youthful_nudisco / 04 digital_hopeful
SEC=$(ffprobe -v error -show_entries format=duration -of csv=p=0 "${T}_9x16(youtube).mp4")  # 길이(ffprobe: exit 0)
ST=$(python3 -c "print(round($SEC-2.5,2))")    # 페이드 시작 = 영상길이-2.5

"$FF" -y -i "${T}_9x16(youtube).mp4" -stream_loop -1 -i "$BGM" \
  -filter_complex "[1:a]afade=t=out:st=${ST}:d=2.5[a]" \
  -map 0:v -map "[a]" -c:v copy -c:a aac -b:a 192k -shortest -movflags +faststart \
  "${T}_9x16(youtube)_bgm.mp4"
```
- `-stream_loop -1`: BGM이 영상보다 짧아도 루프. `-shortest`로 영상 길이에 맞춤.
- `afade=t=out:st=<영상길이-2.5>:d=2.5`: 마지막 2.5초 페이드아웃.
출력: `..._9x16(youtube)_bgm.mp4`

---

## 6. 이미지(PNG)
**표준: ① 대지별 이미지 = 각 클립의 마지막 프레임(애니메이션·강조가 모두 끝난 최종 상태), ② 표지 `_표지.png` 별도.**

### 6-1. 대지별 이미지 = 클립 마지막 프레임 (권장)
개별 클립은 최종 프레임을 홀드하므로, **클립의 마지막 프레임 = 애니메이션 완료 상태**. 별도 렌더 없이 그대로 뽑는다(클립이 1080×1350이라 다운스케일도 불필요).
```bash
cd exports; T="파이 이용가이드 0N_<제목>"
for n in 01 02 03 04 05 06 07 08 09; do
  [ -f "${T}_${n}.mp4" ] && ffmpeg -y -sseof -0.3 -i "${T}_${n}.mp4" -update 1 "${T}_${n}.png"
done   # -sseof -0.3 -update 1 = 끝 0.3초 중 마지막 프레임
```

### 6-2. 표지 이미지 — `tools/export-cover.js`
표지(대지1, idx 0)를 최종 상태로 2배(2160×2700) 캡처 후 lanczos 다운스케일.
```bash
GUIDE=02 node export-cover.js                    # 02 표지(영상 없음)
GUIDE=03 POSTER=.pen.png node export-cover.js     # 03 표지(pen-anim → 정지프레임 치환)
# 다운스케일(2160x2700 → 1080x1350) — 임시파일 확장자는 반드시 .png (ffmpeg가 포맷 인식)
cd exports; T="파이 이용가이드 03_증여세 신고하기"
ffmpeg -y -i "${T}_표지.png" -vf "scale=1080:1350:flags=lanczos" "${T}_표지.sc.png" && mv "${T}_표지.sc.png" "${T}_표지.png"
```
- 표지에 영상이 있으면(예: 03 pen-anim) 정지프레임 포스터로 `<video>`를 치환해 캡처:
  `ffmpeg -ss <초> -i assets/pen-anim.mp4 -frames:v 1 tools/.pen.png` → `POSTER=.pen.png`.
- 04 표지처럼 별도 디자인 커버 PNG가 있으면 그걸 `_표지.png`로 그대로 쓴다.
- (구) 대지별 PNG가 필요할 때만 `tools/export-images.js` 사용(현재 표준 아님).

---

## 7. 파일명 규칙
```
파이 이용가이드 0N_<가이드 제목>.mp4                     # 합본
파이 이용가이드 0N_<가이드 제목>_0M.mp4                  # 대지 0M 개별 클립
파이 이용가이드 0N_<가이드 제목>_0M.png                  # 대지 0M 이미지(클립 마지막 프레임)
파이 이용가이드 0N_<가이드 제목>_표지.png                # 표지 이미지
파이 이용가이드 0N_<가이드 제목>_9x16(youtube).mp4       # 9:16 무음
파이 이용가이드 0N_<가이드 제목>_9x16(youtube)_bgm.mp4   # 9:16 + BGM
```
- 0N = 가이드 번호(02 간편신고, 03 증여세, 04 미국 ETF, 05 세금 없이 물려주기), 0M = 대지 순번.
- **이름은 구글드라이브 폴더명 기준으로 통일**(→ [README](README.md) 2절).

---

## 8. 순서 요약
1. 서버 켜기 → `GUIDE=0N node export-clips.js`로 개별 클립.
2. 표지: `GUIDE=0N node export-cover.js`. 대지별 이미지: 클립 마지막 프레임 추출(6-1).
3~5. `build-combine.sh`로 합본 → 9:16 → BGM(한 번에).
6. `exports/` 최종본 커밋 → `구글드라이브_최종본/` 미러 갱신 → **드라이브 업로드**([가이드](구글드라이브_업로드_가이드.md)).
7. **인스타그램 캡션** 작성([가이드](인스타그램_캡션_가이드.md)) → 각 폴더에 `인스타그램_캡션.md`.
